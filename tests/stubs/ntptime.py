"""Стаб модуля ntptime для запуска main.py под CPython в тестах."""


def settime() -> None:
    """Имитирует успешную NTP-синхронизацию (часы не трогает).

    Raises:
        OSError: Никогда в стабе (реальный модуль кидает при недоступности сети).
    """
    return None
